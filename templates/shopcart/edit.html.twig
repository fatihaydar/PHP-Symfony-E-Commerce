{% extends 'base.html.twig' %}

{% block title %}Edit Shopcart{% endblock %}

{% block body %}
    <h1>Edit Shopcart</h1>

    {{ include('shopcart/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('orders_new') }}">back to list</a>

    {{ include('shopcart/_delete_form.html.twig') }}
{% endblock %}
