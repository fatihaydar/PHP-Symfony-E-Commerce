{% extends 'base.html.twig' %}
{% block leftsidebar %} {{ include('home/userleftsidebar.html.twig') }} {% endblock %}
{% block title %}üye paneli{% endblock %}

{% block content %}
    <div style="float:right;width:60%;">


<div class="section" style="margin-left:1000px ">
    <!-- container -->
    <div class="container" >
        <!-- row -->
        <div class="row" >
    <h1>Profil Bilgileri</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ app.user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ app.user.email }}</td>
            </tr>

            <tr>
                <th>Name</th>
                <td>{{ app.user.name }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ app.user.city }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ app.user.phone }}</td>
            </tr>


        </tbody>

    </table>
            <a href="{{ path('userpanel_edit') }}"class="flex-c-m size2 bg1 bo-rad-23 hov1 m-text3 trans-0-4">Düzenle</a>
        </div></div></div>
    </div>
    <div style="clear:both;"></div>
{% endblock %}
